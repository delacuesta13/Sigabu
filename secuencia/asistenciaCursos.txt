<<controller>>\nProgramaciones-><<view>>\nIndex: index()
<<view>>\nIndex-><<action>>\nListar programaciones: getProgramaciones()
<<view>>\nIndex-><<view>>\nVer: consultar( id )
<<view>>\nVer-><<controller>>\nAsistencia: nuevo( id_curso )
<<controller>>\nAsistencia-><<view>>\nNuevo: nuevo( id_curso )
<<view>>\nNuevo-><<controller>>\nAsistencia: *[para cada campo del formulario] datos:=recoger()
<<controller>>\nAsistencia-><<controller>>\nAsistencia: datosVálidos:=validar_datos( datos )
<<controller>>\nAsistencia-><<model>>\nAsistencia: [datosVálidos] rs_query:=nuevo( datos )
<<controller>>\nAsistencia-><<view>>\nNuevo: nuevo( rs_query )