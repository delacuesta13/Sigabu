<<controller>>\nProgramaciones-><<view>>\nIndex: index()
<<view>>\nIndex-><<action>>\nListar programaciones: getProgramaciones()
<<view>>\nIndex-><<view>>\nVer: consultar( id )
<<view>>\nVer-><<controller>>\nInscripciones: nuevo ( id_curso )
<<controller>>\nInscripciones-><<view>>\nNuevo: nuevo( id_curso )
<<view>>\nNuevo-><<controller>>\nInscripciones: *[para cada campo del formulario] datos:=recoger()
<<controller>>\nInscripciones-><<controller>>\nInscripciones: datosVálidos:=validar_datos( datos )
<<controller>>\nInscripciones-><<model>>\nInscripción: [datosVálidos] rs_query:=nuevo( datos )
<<controller>>\nInscripciones-><<view>>\nNuevo: nuevo( rs_query ) 